name: Import verified production baseline

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  import:
    if: github.event.head_commit.message != 'Import verified production baseline from ZIP'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Import exact ZIP at repository root
        shell: bash
        run: |
          set -euo pipefail
          ZIP="Pakdasht-Inspection-PRODUCTION-GITHUB-ROOT-REGISTRATION.zip"
          test -f "$ZIP"
          rm -rf /tmp/pakdasht-import
          mkdir -p /tmp/pakdasht-import
          unzip -q "$ZIP" -d /tmp/pakdasht-import
          test "$(find /tmp/pakdasht-import -type f | wc -l)" -eq 40
          test -f /tmp/pakdasht-import/api/config.mjs
          test -f /tmp/pakdasht-import/api/server.mjs
          test -f /tmp/pakdasht-import/api/data/pakdasht.sqlite
          test -f /tmp/pakdasht-import/dist/icons/icon-192.png
          test -f /tmp/pakdasht-import/dist/icons/icon-512.png
          test -f /tmp/pakdasht-import/web/icons/icon-192.png
          test -f /tmp/pakdasht-import/web/icons/icon-512.png
          find . -mindepth 1 -maxdepth 1 ! -name .git -exec rm -rf {} +
          cp -a /tmp/pakdasht-import/. .
          rm -f "$ZIP"
          rm -rf .github
          echo "Imported file count: $(find . -type f | wc -l)"
          find . -type f -print | sort
          test -f api/config.mjs
          test -f api/server.mjs
          test -f api/data/pakdasht.sqlite
          test -f dist/icons/icon-192.png
          test -f dist/icons/icon-512.png
          test -f web/icons/icon-192.png
          test -f web/icons/icon-512.png

      - name: Commit imported baseline
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git add -f api/data/pakdasht.sqlite
          git commit -m "Import verified production baseline from ZIP"
          git push origin HEAD:main
